## Architecture Notes

- Clean Architecture with service & route separation
- Async LLM tasks designed via background workers (Celery/Task queues in full version)
- Storage & LLM providers abstracted via interfaces